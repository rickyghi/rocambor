Original prompt: Hi, I need help fixing the game logic for my Rocambor game:

1. Bot always upping ante and playing solo oros
2. When a suit leads a trick and you don’t have any cards of the suit, you can play any card you like. Don’t have to follow suit or use a truimph card necesarily.
3. When a Matador leads a trick any card that is beneath it in rank is forced to be played, including other lower matadors. But not the higher matadors. So a trick lead by the espada, forces all other triumph cards to be played including the second and third matadors. But a trick lead by the third matador doesn’t force the first nor second matadors.

## Compressed Snapshot (2026-03-12)

- Gameplay rules: bots are less eager to force `solo_oros`, void players can discard freely when they cannot follow suit, and connected human turns no longer show countdown timers.
- Client architecture: screen routing is now fully React-owned. `home`, `lobby`, `leaderboard`, `game`, `post-hand`, and `match-summary` all render through React, while the existing canvas renderer remains the current game-table boundary.
- Game table UI: the desktop table was reworked around the Figma direction. Hero plates are simplified, the top bar uses the main Rocambor logo, the announcement HUD was reduced to one main plaque, the hand fan is larger and cleaner, and auction/exchange panels now use compact dark-glass styling.
- Supporting screens: Home and Lobby were polished to better match the Rocambor desktop visual system, the leaderboard now uses a darker editorial treatment, and the profile modal now includes richer standing/stats/achievement context plus recent local match history.
- End-of-play flow: post-hand and match-summary now share the same darker Rocambor finish as the rest of the product, and match summaries are recorded into local profile history so the profile view has real recent results.
- Backend/data plumbing: room names are carried through shared/server state, `/api/players/:playerId/stats` now returns safe defaults for new players, and leaderboard/profile views can render without noisy first-time-user errors.
- Verification status: root type-check and client build pass, and recent browser verification on a fresh local backend/client pair confirmed zero console errors across the polished leaderboard, profile modal, exchange panel, post-hand, and match-summary surfaces.
- Recommended next work: desktop `trump_choice` / `penetro` parity, then remaining in-game popups and finally the mobile-specific polish pass.

2026-03-10: Inspected engine, room auction validation, and bot bidding/play code. Identified two rule mismatches in legalPlays (mandatory trumping when void, mandatory any-trump response to all trump leads) and aggressive bid thresholds/opening behavior in decideBid.
2026-03-10: Updated legalPlays to allow free discard when void in a plain led suit, and to force only lower trumps when a matador leads. Rebalanced bot bidding thresholds and overcall selection so bots only step up when the hand actually qualifies. Verified with server tests, bot simulations, and full repo type-check.
2026-03-10: Tuned trick-play heuristics so bots only lead a top trump when they actually control the trump suit (multiple matadors), and free discards now prefer weak cards from shorter plain suits. Verified with targeted bot tests, bot-vs-bot simulations, and repo type-check.
2026-03-10: Whole-game analysis pass: server tests still pass (8 files / 160 tests), repo type-check passes, and a browser smoke check reached the live client with active game state and no console errors. No client automation harness exists yet.
2026-03-10: Highest-risk findings from verification: auction pass-state bug can re-enable passed bidders and corrupt winner/pass-out resolution; client phase-change handler can recurse through clearSelection()->notify(); JOIN_ROOM auto-seating bypasses TAKE_SEAT live-game guard; spectator/null-hand states do not clear stale client hands; implied bola does not update state.ombre before scoring; bot delayed actions can outlive room cleanup.
2026-03-10: Additional product debt: client expects CARD_PLAYED/DEAL style feedback paths that server does not emit; docs/RULES.md still describes mandatory trumping and defender exchange caps that no longer match engine/exchange-utils; renderer forces continuous repaint while countdown is active and global state notifications trigger expensive full-screen rerenders on every card selection.
2026-03-10: Implemented whole-game stability pass. Server fixes: auction pass-state is now final and turn order skips passed bidders; implied bola now transfers ombre to the continuing player; JOIN_ROOM no longer auto-seats humans into live games; pending bot actions are invalidated on timer resets/cleanup; explicit CARD_PLAYED and DEAL events are emitted; connected humans no longer receive turn deadlines/timeouts (bots and disconnected seats still do).
2026-03-10: Client fixes: hand now clears on hand:null updates; exchange limits are centralized and respect talon caps; mobile contrabola flow no longer allows zero-card submit; solo can be bid plainly without forced early suit declaration; phase-change recursion fixed by updating prevPhase before side effects; trick-result banner timers are debounced; selection-only state changes no longer trigger full heavy UI refreshes; renderer countdown repaint is throttled and DPR listeners are cleaned up; small portrait header guidance remains visible.
2026-03-10: Verification after whole-game pass: server tests pass (8 files / 159 tests), repo type-check passes, and production build passes for both server and client.
2026-03-11: Started React migration on the client. Added React + ReactDOM + Vite React plugin, enabled JSX in the client tsconfig, and replaced the imperative bootstrap in client/src/main.ts with a React app shell.
2026-03-11: Introduced a React-owned route layer that preserves the existing screen contract through legacy wrappers. `game`, `post-hand`, `match-summary`, and `leaderboard` still mount through the old Screen classes, but route state now lives in React and `router.ts` exposes a smaller `RouterHandle` interface instead of coupling AppContext to the old DOM router implementation.
2026-03-11: Migrated `home` and `lobby` to React components under client/src/app/screens. These now render declaratively, use manager subscription hooks instead of manual `innerHTML` + rebinding, and preserve the existing create/join room, quick play, profile, settings, and lobby seat/start flows.
2026-03-11: Verification for the React slice: client type-check passes, client production build passes, root type-check passes, and a browser smoke test on http://127.0.0.1:3000/ successfully rendered the React home screen, created a room, navigated to the React lobby screen, and reported no browser console errors.
2026-03-11: TODO / next safe slice: migrate leaderboard next for a low-risk fetch-driven React screen, or migrate post-hand/match-summary together to remove the remaining modal summary screens from the legacy mount path before touching the canvas-heavy `game` screen.
2026-03-11: Bigger simplification opportunities remain mostly in product/rules scope, not just UI. The current game complexity still comes from multi-phase contracts and exceptions (auction variants, exchange rules, penetro/bola/contrabola, resting-seat logic). If the goal is to simplify gameplay itself, decide which contracts/phases should survive before rewriting the game screen in React.
2026-03-11: Continued the React migration and moved the remaining non-canvas screens into React: leaderboard, post-hand, and match-summary now render through React components under client/src/app/screens. At this point only the canvas-heavy `game` screen still mounts through the legacy Screen wrapper.
2026-03-11: Added a dev `/api` proxy in client/vite.config.ts so the new React leaderboard can talk to the current server during local Vite development. Before that fix, the leaderboard route received index.html from Vite and showed a JSON parse error.
2026-03-11: Verification for the second React slice: client build passes, root type-check passes, the React leaderboard fetches real data in dev, injected dev-state rendered the React post-hand and match-summary screens correctly, and browser console errors remained at zero across home, lobby, leaderboard, post-hand, and match-summary.
2026-03-11: Tooling note: the shared `develop-web-game` Playwright smoke script could not be executed directly because the skill-local script imports `playwright` from its own directory and that package is not installed there. Browser verification was completed with the built-in Playwright MCP instead.
2026-03-11: Next safe slice: migrate the `game` shell itself to React around the existing canvas/controls/renderer, likely by converting DOM overlays (header, banners, hand dock, controls host) before replacing the imperative GameScreen orchestration class.
2026-03-11: Migrated the `game` route into the React app shell as well. React now renders the game DOM markup, and the existing GameScreen controller attaches to that DOM through a new `attach()` path instead of owning `innerHTML` creation. This keeps the current renderer, controls, and event logic intact while removing the final legacy route mount from App.
2026-03-11: A layout regression surfaced during verification: the extra React host wrapper around the game markup changed positioning enough to push action controls out of the viewport. Fixed by rendering the React host with `display: contents`, preserving the original box tree while still giving React a mount node.
2026-03-11: Verification for the game-shell migration: client build passes, root type-check passes, live browser flow reached lobby -> game, settings modal opened from the game header, and a real card click advanced live state (`seq` incremented, card removed from hand, card added to table). Browser console errors remained at zero during the game-shell checks.
2026-03-11: Current boundary: routing and screen composition are fully React-owned. The remaining imperative code is inside the game controller itself (`client/src/screens/game.ts`) and the existing canvas/controls helpers it orchestrates. Next step if continuing is to peel controller responsibilities into React-owned subareas: header/HUD, hero plates, DOM hand/trick layers, then controls.
2026-03-11: Continued the game migration by moving the top chrome into React. `client/src/app/screens/GameTopChrome.tsx` now owns the game header, countdown text, ping/profile/sound/settings actions, HUD pills, and mobile summary, while `client/src/app/screens/GameScreen.tsx` renders the full game shell as JSX instead of injecting legacy markup with `dangerouslySetInnerHTML`.
2026-03-11: Narrowed the legacy controller boundary in `client/src/screens/game.ts`: `attach()` now binds to a React-rendered `.game-screen` root, header/mobile-summary DOM refs and button bindings were removed, and header update work no longer runs on state/settings/latency/resize cycles. The controller still owns the current canvas renderer, hero plates, phase banner, trick overlays, hand dock, and controls.
2026-03-11: Verification for the React top-chrome slice: client type-check passes, client build passes, root type-check passes, the live game route rendered the React header, the settings modal still opened from the React action button, and a real canvas click on a legal hand card advanced gameplay (`seq` 100 -> 101, `o10` moved from hand to table). Browser console errors remained at zero.
2026-03-11: Continued the migration by moving player chrome into React as well. `client/src/app/screens/GamePlayerChrome.tsx` now owns the desktop hero plates, the compact self plate, and the mobile opponents strip, and `client/src/app/screens/GameScreen.tsx` renders those components directly into the game shell instead of leaving empty DOM slots for the legacy controller to populate.
2026-03-11: Narrowed the controller boundary again in `client/src/screens/game.ts`: live state/profile/resize/event flows no longer call the old hero-plate or mobile-opponent renderers, so the controller is effectively down to canvas rendering, phase banner/toasts, DOM hand + trick overlays, and controls orchestration.
2026-03-11: Verification for the React player-chrome slice: client type-check passes, client build passes, root type-check passes, a live auction `Pass` action updated the React hero plates correctly on desktop, a portrait viewport resize rendered the React mobile opponents strip with live seat/card state, and browser console errors remained at zero.
2026-03-11: Continued the migration by moving the sprite-mode card layer into React. `client/src/app/screens/game-dom-layer-bridge.ts` now bridges controller-owned interaction state (sprite mode, pending card, trick overlay, viewport mode, invalid-action shake) into React, and `client/src/app/screens/GameDomLayers.tsx` renders the trick overlay, lead prompt, hand dock, and mobile action button declaratively from client state plus that bridge snapshot.
2026-03-11: Narrowed the controller boundary again in `client/src/screens/game.ts`: the controller no longer writes `innerHTML` into the hand/trick DOM layer, no longer binds DOM card/button listeners there, and now only feeds the bridge while keeping gameplay logic, sprite-mode enable/fallback decisions, phase banner/toasts, canvas rendering, and controls orchestration.
2026-03-11: Verification for the React DOM-layer slice: client type-check passes, client build passes, root type-check passes, the live game still rendered the React hand dock/trick layer shell with zero console errors, and a controlled injected play-state plus mocked `connection.send` verified that clicking a legal React hand card emitted `{ type: "PLAY", cardId: "c10" }` and updated the injected state from hand -> table (`seq` 500 -> 501).
2026-03-11: Continued the migration by moving the central game controls bar into React. `client/src/app/screens/GameControlsBar.tsx` now renders the auction, penetro, trump, exchange, close-hand, rematch, and start-game controls declaratively from `ClientState`, including the old short-lived action lock keyed off `game.seq`, and `client/src/app/screens/GameScreen.tsx` mounts that component directly in the React game shell.
2026-03-11: Narrowed the controller boundary again in `client/src/screens/game.ts`: the React game route no longer exposes `#game-controls`, so the controller no longer owns controls orchestration there and only keeps the old `GameControls` class as a dormant fallback when `mount()` renders legacy markup.
2026-03-11: Verification for the React controls slice: client type-check passes, client build passes, root type-check passes, a live browser flow created a room, started a Tresillo hand, rendered the React auction controls with no `#game-controls` mount node present, and clicking `Pass` advanced the real game state from `Your turn: choose a bid` to waiting on the bot while browser console errors remained at zero.
2026-03-11: Followed with a controller cleanup pass in `client/src/screens/game.ts`, deleting the dead hero-plate, mobile-opponent, HUD-pill, and DOM-card helper paths that React already replaced, plus the stale DOM refs/imports/selection tracking they depended on. The controller now reads much closer to its real responsibilities: canvas, phase banner/toasts, trick feedback, bridge state, and the legacy `GameControls` fallback for `mount()`.
2026-03-11: Verification for the controller cleanup slice: client type-check passes, client build passes, root type-check passes, the live browser reopened the in-progress `#game` route with zero console errors, and clicking a real legal hand card still advanced the game from `Your turn: play a legal card` to `Waiting for Jugador (Bot) to play`, updated trick count `6/9 -> 7/9`, and showed the trick win/result feedback normally.
2026-03-11: Continued the migration by moving the remaining feedback UI into React. `client/src/app/screens/game-feedback-bridge.ts` now carries phase-banner, arena-toast, and trick-result snapshot state, `client/src/app/screens/GameFeedbackOverlays.tsx` renders those overlays declaratively, and `client/src/app/screens/GameScreen.tsx` mounts that feedback layer into the React game shell.
2026-03-11: Narrowed the controller boundary again in `client/src/screens/game.ts`: when a feedback bridge is attached, the controller no longer queries or writes the phase-banner, toast-feed, or trick-result DOM nodes and instead only publishes feedback state while keeping a small legacy DOM fallback for the old `mount()` path.
2026-03-11: Verification for the React feedback slice: client type-check passes, client build passes, root type-check passes, the live browser confirmed the React route no longer exposes `#arena-phase-banner`, `#arena-toast-feed`, or `#trick-result-banner`, and a sampled real play sequence showed the React banner updating across turns, a React toast chip for `Bot wins trick (highest card)` appearing and exiting, and a React trick-result banner `Bot wins with As de Bastos!` showing and animating out with zero browser console errors.
2026-03-11: Removed the final legacy game-screen fallback path from `client/src/screens/game.ts`. The controller no longer exports generated screen markup or a `mount()` path, no longer implements the old `Screen` contract, and now requires React-provided DOM-layer and feedback bridges in `attach()` instead of carrying optional fallback branches for controls, phase-banner DOM nodes, toast feed, or trick-result banner timers.
2026-03-11: Verification for the legacy-path removal slice: client type-check passes, client build passes, root type-check passes, the shared `develop-web-game` harness is still blocked by the skill-local missing `playwright` dependency, and built-in Playwright verification on `http://127.0.0.1:3001/#game` confirmed the React route still runs with zero browser console errors, legacy fallback nodes remain absent (`#game-controls`, `#arena-phase-banner`, `#arena-toast-feed`, `#trick-result-banner`), a real `Pass` action advanced Auction -> Choose Trump, and a real `Keep All` action advanced Exchange -> waiting on the bot.
2026-03-11: Removed the remaining dead router-era client scaffolding. `client/src/router.ts` now only exports the shared route/app-context types used by the React app, `client/src/app/LegacyScreenHost.tsx` was deleted, and the old imperative non-game screen classes (`client/src/screens/home.ts`, `lobby.ts`, `leaderboard.ts`, `post-hand.ts`, `match-summary.ts`) were deleted because nothing in the React client imports them anymore.
2026-03-11: Updated `docs/DESIGN.md` to describe the current React-owned hash routing model instead of the removed `Screen`/`Router` lifecycle contract.
2026-03-11: Verification for the router-cleanup slice: client type-check passes, client build passes, root type-check passes, the shared `develop-web-game` harness is still blocked by the skill-local missing `playwright` dependency, and built-in Playwright verification showed zero browser console errors while navigating `#home` -> `#leaderboard` -> `#game`; a real live card play still advanced the game into the post-hand flow and the session continued into the next hand correctly.
2026-03-11: Addressed the next gameplay slice. Bot bidding is now stricter about `solo_oros`: `server/src/bot.ts` requires a genuinely dominant Oros profile (at least 42 exact trump points and 6 trumps) before overcalling into `solo_oros`, which stops marginal Oros-heavy hands from jumping there just because it is technically available.
2026-03-11: Locked the free-discard rule down at the room layer. `server/tests/room.test.ts` now verifies that when a player is void in the led suit, the state sent to that player exposes every card as legal and an off-suit discard is accepted, matching the lower-level `legalPlays` engine behavior.
2026-03-11: Hardened the no-human-timer behavior end-to-end. `server/src/room.ts` now strips stale `turnDeadline` values from any state broadcast whose active turn belongs to a connected human, and `client/src/state.ts` defensively removes any incoming deadline for connected human turns before the UI renders it.
2026-03-11: Verification for the gameplay slice: focused server tests (`tests/bot.test.ts`, `tests/room.test.ts`) pass, client type-check passes, client build passes, root type-check passes, the shared `develop-web-game` harness is still blocked by the skill-local missing `playwright` dependency, and built-in Playwright verification confirmed that a fake human-turn state drops the countdown while a fake bot-turn state still shows it, both with zero browser console errors.
2026-03-11: Started the first post-migration game-table polish pass in the React shell. `client/src/app/screens/GameFeedbackOverlays.tsx` now collapses phase banner, toast, and trick-result state into a single bottom-left callout HUD; `client/src/app/screens/GameTopChrome.tsx` no longer renders the redundant top-right guidance bubble; `client/src/app/screens/GameDomLayers.tsx` no longer renders the separate lead prompt and now uses larger, more dramatic trick-card offsets and hand fan math; and `client/src/app/screens/GamePlayerChrome.tsx` now separates table position from game role so the desktop hero plates only show position, name, role, and trick diamonds.
2026-03-11: Retuned the table composition in `client/src/screens/game.css` to match the new hierarchy: desktop hero plates are smaller and self-contained, the across plate sits lower below the header, the single bottom-left callout owns announcements, the trick-table cards are larger and more centered, and the self hand now uses a larger, tighter fan so the cards read as the primary foreground element instead of a wide white slab.
2026-03-11: Verification for the first UI/UX polish slice: client type-check passes, client build passes, root type-check passes, built-in Playwright shows zero browser console errors on `http://127.0.0.1:3001/#game`, the old duplicate announcement nodes are gone (`.arena-phase-banner`, `.turn-lead-prompt`, `.game-header-center` absent from the live DOM), the across hero plate now clears the header (`header.bottom = 70`, `hero-across.top = 128`), and the live role lines render the simplified labels `Jugador · Entrada Oros`, `Primer Contra`, `Resting`, and `Segundo Contra`.
2026-03-11: Figma MCP access was restored under `ricardo@smartfuture.la`, and I used the actual desktop play-trick frame (`3:481`) plus the plaque (`3:684`) and self-plate (`3:708`) screenshots to drive the next polish adjustments instead of inferring from screenshots alone.
2026-03-11: The second polish pass kept the user-requested simplified information model but pulled the visual composition closer to Figma. `client/src/screens/game.css` now places the single announcement HUD as a low bottom-left plaque, widens the self plate into a more Figma-like gold bar, lowers the hand fan so the cards enter from the bottom edge instead of reading as a white slab, simplifies the center table ring by removing the extra bordered oval from the trick overlay, and brightens the outer felt ellipse to read more like the Figma table outline.
2026-03-11: Verification for the Figma-informed polish pass: client type-check passes, client build passes, root type-check passes, built-in Playwright on `http://localhost:3000/#game` shows zero browser console errors, and live geometry confirms the callout now sits in the intended bottom-left zone (`top 831, bottom 877`), the self plate stays above the hand (`self.bottom 639`, `hand.top 647`), and the simplified hero plates plus single announcement surface remain intact.
2026-03-11: Landed a third desktop polish pass focused on the remaining card-dock and header issues. `client/src/screens/game.css` now forces the live hand row transparent at the game-screen level (overriding the shared ivory hand container), restores smooth sprite rendering by switching `.roc-card` back to `image-rendering: auto`, and adds a dark bottom dock gradient so the self hand emerges from a Figma-like table band instead of a light tray.
2026-03-11: Polished the top bar in `client/src/app/screens/GameTopChrome.tsx` and `client/src/screens/game.css` using the desktop header node (`3:860`) as reference: the header is now a dark glass bar with a centered HUD shell, a branded Rocambor lockup on the left, reordered action icons, and a profile block that shows the live room code as a small uppercase kicker under the player name.
2026-03-11: Verification for the header/card-dock polish pass: client type-check passes, client build passes, root type-check passes, built-in Playwright on `http://localhost:3000/#game` shows zero browser console errors, the live hand row now computes to `background: rgba(0, 0, 0, 0)` instead of the old ivory strip, live card sprites compute to `image-rendering: auto`, and the refreshed viewport confirms the white container is gone while the branded header still clears the play area.
2026-03-11: Applied the next desktop table-tuning pass based on live screenshots. `client/src/screens/game.css` now lets hand-card wrappers overflow visibly, raises hovered/selected/legal cards above the fan, increases desktop hand-row side padding, and keeps the play-phase self banner lower so it doesn’t sit in the played-card lane.
2026-03-11: Updated the desktop table-card scale in two places: `client/src/screens/game.css` now sets larger `--trick-card-*` tokens at the game-screen level, and `client/src/app/screens/GameDomLayers.tsx` pushes the desktop trick slots outward/upward so the enlarged cards still sit around the center table ring cleanly.
2026-03-11: Swapped the top-left brand lockup to the main precomposed Rocambor logo in `client/src/app/screens/GameTopChrome.tsx`, removing the temporary coin-plus-text treatment.
2026-03-11: Verification for the table-card/self-banner/logo pass: client build passes, root type-check passes, built-in Playwright on `http://localhost:3000/#game` shows zero browser console errors, live hand-card wrappers now compute to `overflow: visible`, the full logo asset is mounted at `/assets/rocambor/logo-light.png`, the play-phase self banner now sits at `top 580` instead of the previous `530`, and a same-tick local play-state probe measured the enlarged trick-card box at `173x223`.
2026-03-11: Tightened the play-hand geometry again. `client/src/screens/game.css` now makes the desktop hand slightly larger in both normal and play sizing, removes wrapper-border highlighting in favor of centered `.roc-card` halos for selected/legal/pending cards, and gives the play fan a little more side padding so the larger cards do not crowd the dock edges.
2026-03-11: Moved the trick-play geometry again in `client/src/app/screens/GameDomLayers.tsx` and `client/src/screens/game.css`: the across trick card now sits much lower than before, and the play-phase self banner is pushed further down so it no longer sits directly on top of the self trick lane.
2026-03-11: Verification for the hand/halo/across-card pass: client type-check passes, client build passes, root type-check passes, built-in Playwright shows zero browser console errors, a local play probe measured the across hero plate clearing the enlarged top card (`plate.bottom 250`, `card.top 257`), the self banner now sits at `top 588`, and the play-hand probe measured the larger hand card at roughly `166x192` with visible overflow and card-centered highlighting.
2026-03-11: Fixed a stale phase-class bug in `client/src/screens/game.ts`. The React shell could show `phase-play` styling even after the underlying room state had moved to `exchange` or `auction`, because the game controller only refreshed shell classes when `seq`, seat, or hand changed. The controller now also tracks `lastObservedPhase`, so phase-only changes correctly re-run `syncPhaseClass()`, transitions, and banner refreshes.
2026-03-11: Verification for the phase-class + compact-controls pass: client type-check passes, client build passes, root type-check passes, built-in Playwright on `http://localhost:3000/#game` shows zero browser console errors, forced local exchange/auction probes now switch the root to `phase-exchange` / `phase-auction` correctly, the selected hand-card halo is centered on the sprite card (`left/right inset 1.2px`, `top/bottom inset 3.85px`), the compact exchange panel measures about `228x124`, and the desktop auction panel measures about `336x316` with muted per-action colors instead of the earlier oversized dominant treatment.
2026-03-11: Restyled the desktop action panels in `client/src/app/screens/GameControlsBar.tsx` and `client/src/screens/game.css`. Auction, exchange, trump-choice, and penetro now use the same darker glass-on-felt language as the rest of the table instead of the bright parchment block, the auction/exchange quote footer was removed, and the controls shell sits slightly lower so the panels read like table utilities rather than modal overlays.
2026-03-11: Added a clearer desktop control hierarchy. Auction options now carry distinct but muted per-bid accents (`entrada`, `volteo`, `oros`, `solo`, `solo_oros`, `pass`, `contrabola`), exchange actions use separate confirm/keep/defer tones, and `GameControlsBar` now exposes panel kind plus grid counts so the compact exchange panel can lay out 2 or 3 actions cleanly without oversized buttons.
2026-03-11: Verification for the desktop auction/exchange polish pass: client type-check passes, client build passes, root type-check passes, built-in Playwright shows zero browser console errors, a forced desktop auction probe measured the new auction panel at `368x315.7` with bid cards around `163x112`, and a forced desktop exchange probe measured the exchange panel at `346x150.9` with action buttons around `154x65`. Screenshots captured during the probe (`tmp/auction-after.png`, `tmp/exchange-after.png`) confirm the darker compact panels no longer overpower the table or self hand.
2026-03-11: Polished the desktop Home screen in `client/src/app/screens/HomeScreen.tsx` and `client/src/screens/home.css`. The external footer bar is gone, the connection indicator now lives at the bottom of the main panel, and the settings/profile utilities now sit on one aligned horizontal row instead of feeling like a separate bottom dock.
2026-03-11: Reshaped the desktop Lobby around the Figma hierarchy in `client/src/app/screens/LobbyScreen.tsx` and `client/src/screens/lobby.css`. The screen now shows a proper room-title block above the salon frame, a stronger room-code header, lighter seat plaques, a cleaner match-configuration section, and helper cards beneath the panel. The room create flow is also ready to carry optional `roomName` through the protocol and room state via `shared/types.ts`, `server/src/protocol.ts`, `server/src/room-router.ts`, `server/src/server.ts`, and `server/src/room.ts`.
2026-03-11: Verification for the Home/Lobby polish pass: client type-check passes, client build passes, root type-check passes, and built-in Playwright shows zero browser console errors. Home now renders without `.home-footer`, exposes two aligned utility buttons in `.home-bottom-bar`, and shows `Connected to the salon server` inside the main panel. Desktop screenshots were captured at `tmp/home-after.png` and `tmp/lobby-after.png`. The live browser session needed a local `roomName` state injection to verify the new lobby title visually because the already-running backend process had not been restarted to broadcast the new `roomName` field yet.
2026-03-12: Tightened the desktop auction/exchange panels again in `client/src/app/screens/GameControlsBar.tsx` and `client/src/screens/game.css`. The auction copy is shorter and more directive, the bid/exchange actions now use restrained per-action accents on dark glass cards, the quote footer stays removed, and the controls shell uses phase-specific placement so auction and exchange sit in the table center without colliding with the self hero plate.
2026-03-12: Verification for the final desktop auction/exchange pass: client type-check passes, client build passes, root type-check passes, and built-in Playwright shows zero browser console errors. Forced local probes on `http://localhost:3000/#game` measured the desktop auction panel at roughly `404x287` with a `36px` gap above the self plate, and the exchange panel at roughly `360x154` with a `76px` gap above the self plate. Final screenshots were captured at `tmp/auction-polish-desktop-final.png` and `tmp/exchange-polish-desktop-final.png`.
2026-03-12: Fixed the remaining verification regressions across the polished desktop surfaces. `client/src/screens/game.css` now hard-centers the compact exchange grid with fixed 2- and 3-action widths so the action buttons sit evenly inside the panel, `client/src/components/profile/ProfileModal.ts` now uses a single avatar-seed import path and the new profile dashboard is stable in the live app, and `client/src/app/screens/MatchSummaryScreen.tsx` now records recent match summaries into `client/src/lib/profile-history.ts` so the polished profile modal can show actual recent results.
2026-03-12: Hardened the profile stats API for first-time players in `server/src/server.ts`. `GET /api/players/:playerId/stats` now returns an empty default stats payload instead of a 404 when no persisted player record exists yet, which keeps the profile UI quiet and consistent on a fresh browser/profile.
2026-03-12: Integrated the leaderboard and end-screen polish into the verified desktop flow. The React leaderboard now uses the darker editorial Rocambor treatment from the Figma direction (`client/src/app/screens/LeaderboardScreen.tsx`, `client/src/screens/leaderboard.css`), and the post-hand/match-summary screens now share the newer dark gold finish (`client/src/app/screens/PostHandScreen.tsx`, `client/src/screens/post-hand.css`, `client/src/app/screens/MatchSummaryScreen.tsx`, `client/src/screens/match-summary.css`).
2026-03-12: Verification for the leaderboard/profile/end-screen slice: root type-check passes, client build passes, and built-in Playwright against a fresh local pair (`server` on `127.0.0.1:8081`, `client` on `127.0.0.1:4174`) shows zero browser console errors. The compact exchange grid now measures centered inside its panel (`centerOffset = 0`), the top-left Rocambor logo measures about `194x45`, the polished profile modal renders with live stats defaults and recent matches, injected match-end state records a recent result (`room-match-end:9:0`), the refreshed profile modal shows that new recent match row, the leaderboard renders the editorial podium/table layout, and the polished post-hand/match-summary routes render correctly under injected state.
2026-03-12: Fixed a React route race in the room-entry flow. `client/src/connection.ts` now records the joined room code immediately on `ROOM_JOINED` via `ClientState.setRoomJoin()`, and `client/src/app/screens/LobbyScreen.tsx` waits for the first `STATE` payload instead of bouncing back to `#home` when `game` is still null for a moment. The lobby now shows a short dark loading state while that initial snapshot arrives.
2026-03-12: Refreshed the settings modal to match the current dark Rocambor surfaces. `client/src/ui/settings-modal.ts` now opens with `modal-dark settings-modal-dialog`, uses card-like toggle rows plus a richer skin showcase/selector, and `client/src/styles/components.css` contains the new dark dialog/mobile responsive styles. This replaces the older ivory generic modal shown from Home/Game.
2026-03-12: Verification for the room-entry + settings pass: `client` type-check passes, and an elevated Playwright smoke run against the live local dev pair (`client` on `127.0.0.1:3000`, `server` on `127.0.0.1:8080`) confirmed `Create Room` now lands on `#lobby`, the settings modal renders with dark glass styling (`backgroundColor rgba(15, 18, 15, 0.95)`), and browser console errors stayed at zero. Artifacts: `output/playwright/home-lobby-settings/lobby-after-create.png` and `output/playwright/home-lobby-settings/settings-modal.png`.
2026-03-12: Used the Figma mobile auction (`NnRJzIEqu2mAMRDHnBw2rc`, node `10:223`) and mobile play (`q4yNDyVjQNEREPorA9Dh3g`, node `10:2`) frames as the reference for the next mobile controls pass. The key takeaways were: the control row should feel embedded in the bottom dock instead of floating in a separate panel, auction should keep a strong 4-button rhythm when four bids are available, and exchange should show one compact selection-status cue above the action row.
2026-03-12: Polished the mobile auction/exchange controls in `client/src/app/screens/GameControlsBar.tsx` and `client/src/screens/game.css`. The shell now exposes the active phase for CSS, exchange confirm labels reflect the number of selected cards (`Trade 2`), mobile auction/exchange strips are dock-embedded instead of dark floating cards, the button grids are count-aware (2/3/4 actions), exchange keeps one small live status chip, and the button colors now better match the Figma rhythm: gold entry, dark green table actions, ivory solo/keep, and crimson pass.
2026-03-12: Verification for the mobile controls pass: `client` type-check passes, and an elevated Playwright mobile run at `393x852` against the live local pair (`127.0.0.1:3000` + `127.0.0.1:8080`) confirmed auction buttons render at `46px` height in the dock strip, exchange status now reads cleanly (`2/3 selected`), exchange actions render as `Trade 2` and `Keep All`, and browser console errors remained at zero. Artifacts: `output/playwright/mobile-controls-polish/auction-mobile-controls.png` and `output/playwright/mobile-controls-polish/exchange-mobile-controls.png`.
2026-03-12: Shipped the full mobile gameplay/settings polish to production in commit `8b91cc3744380654cfa3c9dd3a9aaf96d573f26f` (`Polish mobile gameplay flow and settings UI`). GitHub `main` now includes the mobile viewport unification, larger trick/self-hand emphasis, room-entry lobby fix, dark settings modal refresh, and the Figma-informed mobile auction/exchange dock controls pass.
2026-03-12: Release verification + deploy: `server` test suite passed (`165` tests via `npx vitest run`), `client` type-check passed, production client build passed with `VITE_WS_URL=wss://rocambor-server-production.up.railway.app` and `VITE_API_URL=https://rocambor-server-production.up.railway.app`, and `server` build passed. Production deploys are live on Netlify (`https://rocambor.app`, deploy `69b3274c1884b61f0b825933`) and Railway (`rocambor-server`), and `/healthz` returned `{"status":"healthy"}` at `2026-03-12T20:53:20.381Z`.
2026-03-12: Landed the next mobile gameplay hardening pass before the next ship. `client/src/app/App.tsx`, `client/src/app/screens/GameScreen.tsx`, and `client/src/screens/game.ts` now preload sprite assets and start the React game shell in optimistic sprite mode so the opening game view no longer flashes old canvas/UI chrome before the DOM card layers take over.
2026-03-12: Cleaned up the mobile play surface in `client/src/app/screens/GameDomLayers.tsx`, `client/src/app/screens/GameTopChrome.tsx`, `client/src/screens/game.css`, and `client/src/styles/components.css`: the hand dock now supports drag-scrolling and no longer wastes space on a disabled play CTA, mobile card transforms are centered correctly again, the compact top-bar pills are shorter and scroll instead of compressing into unreadable text, the mobile trick halo treatment is removed, the leave button is visible on both desktop and mobile, and the dark profile modal footer no longer falls back to an ivory mobile bar.
2026-03-12: Hardened the leave-game flow on the server in `server/src/room.ts` so an active seated human who leaves is replaced immediately by a bot, and added coverage in `server/tests/room.test.ts`.
2026-03-12: Verification for the mobile shell + leave-flow pass: `client` type-check passes, the full `server` Vitest suite passes (`166` tests), and injected Playwright iPhone 15 viewport checks (`393x852`) rendered the updated play/auction/trump mobile layouts plus the dark profile modal without browser console errors. Artifacts: `output/playwright/mobile-ui-fixes/play-mobile.png`, `output/playwright/mobile-ui-fixes/auction-mobile.png`, `output/playwright/mobile-ui-fixes/trump-mobile.png`, and `output/playwright/mobile-ui-fixes/profile-modal-mobile.png`.
2026-03-12: Corrected the desktop self-hand size relationship and self-plate drift reported after the Spanish pass. `client/src/styles/theme.css` now makes the shared desktop hand tokens `164x236` in auction and `196x282` in play (about 19.5% larger in play), and `client/src/screens/game.css` now lifts the play hand within its own row instead of dragging the whole dock while applying a small `-12px` play-phase self-slot correction so the self hero plate stays on the same vertical baseline as auction.
2026-03-12: Verification for the desktop hand/hero consistency pass: `client` type-check passes, and a live Playwright desktop probe against the local dev pair (`127.0.0.1:3001` client, `127.0.0.1:8080` server) measured matching self-plate positions in auction and play (`heroTop 803.45` in both states) while the play hand expanded from `164x236` to `196x282`. Artifacts: `output/playwright/desktop-hand-hero-balance/auction-desktop.png` and `output/playwright/desktop-hand-hero-balance/play-desktop.png`.
2026-03-12: Applied a focused mobile overlap/slider cleanup in `client/src/app/screens/GameDomLayers.tsx`, `client/src/screens/game.css`, and `client/src/styles/components.css`. The hand row now has explicit touch-drag fallback listeners in addition to pointer dragging, the duplicate mobile exchange CTA in the bottom gold plaque is removed, trump and penetro now use the same dock-embedded mobile control treatment as auction/exchange, actionable mobile phases reserve vertical space above the hand so the control strip no longer sits directly on the cards, mobile game toasts are moved out of the bottom card lane while the game screen is active, and the mobile self trick slot was lifted so played cards stop collapsing as deeply into the self-hand band.
2026-03-12: Verification for the mobile overlap cleanup: `client` type-check passes, and a Playwright iPhone 15-style probe against the local dev pair (`127.0.0.1:3001` client, `127.0.0.1:8080` server) produced cleaner auction/trump/exchange/play screenshots with the duplicate yellow exchange bar gone and the trump/exchange controls separated from the hand. The injected play probe reduced self-card/hand overlap from about `87px` to about `47px`. Caveat: the emulator still reports a `970px` hand-row width on the iPhone preset, so touch overflow could not be numerically verified here even though touch-drag fallback is now wired in. Artifacts: `output/playwright/mobile-overlap-fixes/auction-mobile.png`, `output/playwright/mobile-overlap-fixes/trump-mobile.png`, `output/playwright/mobile-overlap-fixes/exchange-mobile.png`, and `output/playwright/mobile-overlap-fixes/play-mobile.png`.
2026-03-12: Added bilingual client support with an `en` / `es-ES` toggle on Home and localized the main user-facing flow across Home, Lobby, live gameplay, settings, profile, post-hand, match summary, and leaderboard. Spain Spanish wording is now the baseline Spanish locale, and the Home utility area was tightened so the smaller language toggle sits beneath the settings/profile row instead of competing with the main actions.
2026-03-12: Final mobile polish pass before ship: opening-game sprite mode is preloaded sooner to avoid the old UI flash, the mobile hand dock now supports drag-scrolling and touch-drag fallback, top-bar pills stay readable, duplicate/overlapping mobile overlays were removed, leave-game now replaces the player with a bot, and the Home/PWA install branding now uses the new gold coin artwork for the favicon, Apple touch icon, and manifest icons.
2026-03-12: Release verification + deploy for the latest mobile/spanish/home-branding pass: `client` type-check passed, production client build passed with `VITE_WS_URL=wss://rocambor-server-production.up.railway.app` and `VITE_API_URL=https://rocambor-server-production.up.railway.app`, `server` build passed, and the full `server` Vitest suite passed (`167` tests). Shipped to GitHub in commit `1b647a9` (`Polish mobile game UI and home install branding`), deployed to Netlify (`https://rocambor.app`, deploy `69b363a5f7a45508e8a25a70`), and Railway `/healthz` returned `{"status":"healthy"}` at `2026-03-13T01:09:39.196Z`.
2026-03-13: Started the Supabase/account foundation. The client now includes an optional Supabase auth manager with Home sign-in entry points, the server can verify Supabase access tokens and issue short-lived WebSocket tickets, and authenticated sockets now reconnect/join with account identity while guest/free-play behavior remains intact when Supabase env vars are absent.
2026-03-13: Added persistent authenticated profile/settings support on top of the existing player model. `players.auth_user_id` now links app players to `auth.users`, `/api/me` and `/api/me/profile` expose server-backed profile data, and the React app hydrates/autosaves signed-in profile + settings while preserving the local guest fallback for unsigned users.
2026-03-13: Implemented the first friendly-stakes layer. The server now supports a non-cash friendly-token wallet with starter balances, rescue refill, token ledger entries, and stake-aware room/queue metadata. Quick play and room creation can now opt into `stakeMode: "tokens"`, staked entry is gated to signed-in funded players, token rooms fund antes at match start and settle winner-takes-pot at match end, and the Home screen now exposes the free vs staked choice plus wallet/rescue UI for signed-in users.
2026-03-13: Added persistent match history on top of the account/stakes work. New `matches` and `match_participants` tables record completed matches independently of room reuse/rematches, `/api/me/matches` now returns recent authenticated history, and the profile modal prefers server-backed recent matches for signed-in users while guests continue to use the existing local browser history fallback.
2026-03-13: Verification for the account/stakes/history milestone: `server` Vitest passes (`173` tests), both `client` and `server` type-check pass, and both production builds pass locally. New migrations required before deploy are `003_add_account_profile_settings.sql`, `004_add_friendly_tokens.sql`, and `005_add_match_history.sql`.
2026-03-13: Shipped the current milestone to production in commit `4092e08` (`Add Supabase accounts and friendly stakes foundation`). Netlify production is live at `https://rocambor.app` (deploy `69b422003e1516a4009464dd`) and Railway deployed `rocambor-server` successfully with healthy `/healthz` response at `2026-03-13T14:42:00.383Z`.
2026-03-13: Post-deploy follow-up: fixed the local migration build path by compiling `server/scripts/**/*.ts` in `server/tsconfig.json`, but the linked Railway `rocambor-server` environment currently exposes no `DATABASE_URL` / `POSTGRES_URL`, so migrations `003`–`005` could not be run there yet. The production app remains healthy and free-play safe, but the new authenticated profile, wallet, stakes, and match-history persistence features will stay effectively dormant in production until a database connection is configured for that service environment.
